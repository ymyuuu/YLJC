name: Run YLJC

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-LDAL:
    runs-on: macos-latest  # 使用 macOS 环境

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests selenium webdriver-manager fake-useragent

      - name: Run YLJC
        env:
          BARK_API_URL: ${{ secrets.BARK_API_URL }}
          TRAFFIC_API_URL: ${{ secrets.TRAFFIC_API_URL }}
          LOGIN_URL: ${{ secrets.LOGIN_URL }}
          PLAN_8_URL: ${{ secrets.PLAN_8_URL }}
          PLAN_9_URL: ${{ secrets.PLAN_9_URL }}
          EMAIL: ${{ secrets.EMAIL }}
          PASSWORD: ${{ secrets.PASSWORD }}
        run: |
          python YLJC.py
